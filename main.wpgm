import wollok.game.*

import pepita.*

program PepitaGame {
	game.title("Pepita")
	game.height(10)
	game.width(10)
	game.cellSize(100)

	game.addVisual(pepita)

	keyboard.w().onPressDo({ pepita.fly(1) })

	game.start()
}
